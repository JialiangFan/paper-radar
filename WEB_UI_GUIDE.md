# Web UI 使用指南

## 快速开始

### 1. 启动 Web UI

```bash
# 方式一：使用脚本
./start_web_ui.sh

# 方式二：直接运行
conda activate paper_agent
python web_ui.py
```

### 2. 访问界面

在浏览器中打开：**http://localhost:5001**

## 功能说明

### 📋 关键词管理

- **查看关键词**：页面顶部显示所有已配置的关键词
- **添加关键词**：
  1. 在输入框中输入新关键词
  2. 点击"添加关键词"按钮或按回车键
  3. 关键词会自动保存到 `keywords.txt` 文件
- **删除关键词**：
  1. 点击关键词标签右侧的 `×` 按钮
  2. 确认删除
  3. 关键词会从文件中移除

### 🔍 主动搜索

- **搜索论文**：
  1. 在"主动搜索"区域的输入框中输入关键词
  2. 点击"搜索论文"按钮或按回车键
  3. 系统会立即搜索 ArXiv 并显示结果
- **搜索结果**：
  - 显示论文标题、作者、日期
  - 显示论文摘要（前200字符）
  - 自动生成 AI 总结（使用 gpt-5-nano-2025-08-07 模型）
  - 搜索结果自动保存到数据库

### 📚 已保存的论文

- **查看论文**：页面底部显示最近保存的论文
- **论文信息**：
  - 论文标题（可点击跳转到 ArXiv）
  - 作者、日期、关键词
  - 论文摘要
  - AI 生成的总结
- **自动更新**：搜索新论文后，列表会自动刷新

## 界面特点

- 🎨 **现代化设计**：渐变背景、卡片式布局
- 📱 **响应式**：适配不同屏幕尺寸
- ⚡ **实时更新**：无需刷新页面
- 💾 **自动保存**：所有操作自动保存

## 注意事项

1. **首次搜索**：第一次搜索某个关键词时，可能需要一些时间来生成 AI 总结
2. **缓存机制**：已搜索过的论文会使用缓存的总结，不会重复调用 API
3. **去重功能**：已存在的论文不会重复搜索
4. **API 限制**：注意 OpenAI API 的使用限制和费用

## 故障排除

### 无法启动服务器

- 检查是否安装了 Flask：`pip install flask`
- 检查端口 5001 是否被占用
- 检查 conda 环境是否正确激活

### 搜索失败

- 检查网络连接
- 检查 OpenAI API Key 是否正确配置
- 查看终端输出的错误信息

### 关键词无法保存

- 检查 `keywords.txt` 文件的写入权限
- 检查文件路径是否正确

## 技术栈

- **后端**：Flask (Python)
- **前端**：HTML + CSS + JavaScript (原生，无框架)
- **数据库**：SQLite
- **API**：ArXiv API, OpenAI API

